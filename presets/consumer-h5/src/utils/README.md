# utils

放置跨页面、跨组件复用的纯函数和轻量工具，例如格式化、校验、日期金额处理、URL 参数、安全区/平台能力适配和无副作用数据转换。

- 页面、组件、composable、service 中出现第二次相同工具逻辑时，应收敛到本目录。
- 与 Vue 生命周期、响应式状态或副作用强相关的逻辑放 `src/composables/`，不要放到 utils。
- 与接口字段、数据源和缓存策略强相关的适配放 `src/repositories/` 或 `src/services/`。
- 工具函数应优先保持纯函数、显式入参、稳定返回值，并补充单元测试。
