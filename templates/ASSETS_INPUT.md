# 资产输入

## 用途

保存品牌图片、图标、字体、切图和其他项目素材。

## 命名与版本

建议使用 `场景-名称-版本.ext`，例如 `brand-logo-v1.png`。替代旧资产时在 `manifest.yaml` 记录 `supersedes`。

## 优先级

项目提供资产优先于脚手架默认示例。不得使用随机渐变、通用占位图或字符图标替代已提供资产。

## 修改规则

原始资产默认只读。压缩、裁剪或格式转换应输出到项目源码资产目录，并记录来源与快照。
