# 输入目录

`.fe-harness/inputs/` 保存项目提供的原始证据文件，默认只读。实现过程中不得直接修改、格式化或覆盖原始输入。

## 子目录

- `prd/`：产品需求输入。
- `rp/`：低保真原型、交互草图、线框图和流程原型。
- `ui/`：高保真 UI、截图、标注稿、切图、图标和品牌资产。
- `api/`：OpenAPI、接口文档和接口截图。
- `assets/`：品牌、图片、字体、图标和其他素材。

所有输入必须登记到 `manifest.yaml`，并记录版本、状态、来源和哈希。原始输入发生变化后，不得继续静默使用旧分析结论。
